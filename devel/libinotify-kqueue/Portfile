# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           LegacySupport 1.0


name                libinotify-kqueue

fetch.type          git
git.url             https://github.com/libinotify-kqueue/libinotify-kqueue.git

version             20211018
revision            0
categories          devel
license             MIT
maintainers         nomaintainer
description         The purpose of this library is to provide inotify API on the *BSD family of \
                    operating systems. The library uses kqueue(2) to monitor the file system activity.

long_description    The purpose of this library is to provide inotify API on the *BSD family of \
                    operating systems. The library uses kqueue(2) to monitor the file system activity. \
                    The library is almost feature-complete, i.e. it implements and provides most of what \ can be implemented/provided using kqueue(2).
homepage            https://github.com/libinotify-kqueue/libinotify-kqueue

platforms           darwin
depends_build       port:automake \
                    port:libtool 

depends_lib         port:libevent \
                    port:libgcrypt \
                    port:protobuf-c 

use_autoreconf      yes
autoreconf.args     -fvi

build.cmd           make

#destroot.destdir   /opt/local/lib/libinotify-kqueue

destroot.args       INSTALL_PREFIX=${destroot}${prefix}
